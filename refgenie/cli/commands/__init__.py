"""CLI command families: one module per family, holding models and handlers.

Docstrings are omitted from the command models throughout this package
deliberately: pydantic-settings renders a model docstring as the
subcommand's help description, and these commands document themselves
through their field descriptions and SUBPARSER_MESSAGES instead.
"""
